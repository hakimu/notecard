<%= form_with(model: note, local: true) do |form| %>
  <% if note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :method %>
    <%= form.text_field :method, id: :note_method %>
  </div>

  <div class="field">
    <%= form.label :term %>
    <%= form.text_field :term, id: :note_term %>
  </div>

  <div class="field">
    <%= form.label :code_sample %>
    <%= form.text_area :code_sample, id: :note_code_sample %>
  </div>

  <div class="field">
    <%= form.label :definition %>
    <%= form.text_area :definition, id: :note_definition %>
  </div>

  <div class="field">
    <%= form.label :language %>
    <%= form.text_area :language, id: :note_language %>
  </div>

  <div class="field">
    <%= form.label :concept %>
    <%= form.text_area :concept, id: :note_concept %>
  </div>

  <div class="field">
    <%= form.label :difficulty %>
    <%= form.number_field :difficulty, id: :note_difficulty %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
